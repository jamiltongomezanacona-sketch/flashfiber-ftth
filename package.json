{
  "name": "flashfiber-ftth",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm run build:js && npm run build:geojson && node scripts/copy-static-to-dist.js && node scripts/cache-bust-dist.js && node scripts/write-supabase-env.js && node scripts/write-config-production.js",
    "prepare": "node scripts/ensure-supabase-env.js",
    "build:js": "npx vite build",
    "build:geojson": "node scripts/build-consolidado-geojson.js",
    "migrate:firebase-to-supabase": "node scripts/migrar-firebase-to-supabase.js",
    "export:firestore": "node scripts/exportar-firestore-a-json.js",
    "list:fa-icons": "node scripts/list-fontawesome-icons.js"
  },
  "devDependencies": {
    "dotenv": "^17.3.1",
    "firebase-admin": "^13.0.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.99.1"
  }
}
